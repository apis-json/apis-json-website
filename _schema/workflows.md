---
name: Workflows
description: An optional collection of multi-step API workflows that apply to the whole index (top level) or to an individual API. Each entry uses the same shape as a property (type, url or data, name, description, tags), with a type such as `Arazzo`, `ServerlessWorkflow`, or `Workflow`. Introduced in 0.21.
image: /images/schema.png
url: #
tags:
  - Optional
  - Collection
---
